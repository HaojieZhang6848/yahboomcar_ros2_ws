from setuptools import find_packages, setup

setup(
    name='Speech_Lib',
    version='0.0.1',
    author='Yahboom Team',
    packages=find_packages(),
)

# cd py_install
# sudo python3 setup.py install
